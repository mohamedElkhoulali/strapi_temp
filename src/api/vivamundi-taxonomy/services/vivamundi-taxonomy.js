'use strict';

/**
 * vivamundi-taxonomy service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vivamundi-taxonomy.vivamundi-taxonomy');
