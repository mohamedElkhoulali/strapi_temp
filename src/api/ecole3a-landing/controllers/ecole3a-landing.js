'use strict';

/**
 * ecole3a-landing controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::ecole3a-landing.ecole3a-landing');
