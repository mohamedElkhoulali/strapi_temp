'use strict';

/**
 * igefi-crm-landing service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::igefi-crm-landing.igefi-crm-landing');
