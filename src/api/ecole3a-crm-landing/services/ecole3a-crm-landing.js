'use strict';

/**
 * ecole3a-crm-landing service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ecole3a-crm-landing.ecole3a-crm-landing');
