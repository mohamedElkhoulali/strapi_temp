'use strict';

/**
 * ecole3a-landing-job service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ecole3a-landing-job.ecole3a-landing-job');
